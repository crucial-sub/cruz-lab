rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 모든 이미지 파일에 대해
    match /{allPaths=**} {
      // 읽기는 누구나 가능
      allow read: if true;
      // 쓰기는 인증된 사용자만 가능
      allow write: if request.auth != null;
    }
  }
}
