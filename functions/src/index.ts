// Legacy note:
// The blog publishing flow is now handled by Astro server endpoints that write
// markdown files to GitHub directly. The previous Firestore -> GitHub mirroring
// function is intentionally retired.
export {};
